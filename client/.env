# # ensure the dev socket uses localhost and the default path
# WDS_SOCKET_HOST=localhost
# WDS_SOCKET_PATH=/ws
# # let CRA pick the port dynamically (avoids “invalid frame header” if a mismatch)
# WDS_SOCKET_PORT=0